# Core agent stack
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
langsmith>=0.1.0

# UI
gradio>=4.40.0

# Data
pandas>=2.0.0
openpyxl>=3.1.0

# Schemas
pydantic>=2.5.0

# MCP — official package (NOT langchain-mcp-adapters, that doesn't exist on PyPI)
mcp>=1.0.0
